<%= form_with(url: '/search') do |f| %>
  <%= f.text_field :q %>
  <%= f.submit 'search' %>
<% end %>

<% if @results.any? %>
  <ul>
  <% @results.each do |r| %>
    <li><%= r["webTitle"] %></li>
  <% end %>
  </ul>
<% else %>
  <p>No results</p>
<% end %>



